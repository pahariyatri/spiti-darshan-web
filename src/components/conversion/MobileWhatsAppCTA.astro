---
import WhatsAppLink from './WhatsAppLink.astro';

interface Props {
  route?: string | null;
}
const { route } = Astro.props;
---

<div class="mobile-book">
  <WhatsAppLink class="button button-whatsapp" cta="mobile-bar" route={route}>
    Plan my ride on WhatsApp ↗
  </WhatsAppLink>
</div>
